# План розробки

## Фаза 0 — Фундамент (перш ніж будь-яка "розумна" логіка)
Мета: скелет, що вміє надійно зберігати дані. Без цього аналіз будувати нема
на чому.

- [x] Обрати стек (мова, БД, оркестрація) — зафіксувати в CLAUDE.md
- [x] Базова схема БД: сирі дані, метадані джерел, лог релізів
      (db/schema.sql: raw_observations, sources, release_log)
- [x] Один-єдиний адаптер джерела даних (FRED, CPI/fed_funds_rate/
      unemployment_rate) — наскрізно, від збору (fetch) до нормалізації
      (normalize) і збереження (common/db.py)
- [x] Налаштування Telegram (reporting/telegram_notify.py, потребує
      TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID в .env)
- [x] Мінімальний тест: дані зібрались → лежать у БД → їх можна дістати
      та сформувати і відправити повідомлення останніх свіжих даних.
      Живий наскрізний прогін (run_collect.py → реальна TimescaleDB →
      telegram_notify.py) виконано і підтверджено локально 2026-08-29.

**Критерій завершення фази:** можна руками запустити збір одного показника
і побачити результат у БД. Нічого розумного ще не робимо.

## Фаза 1 — Збір даних (ширина, не глибина)
- [x] Макро-дані: 3-5 ключових показників (інфляція, ставки, безробіття, ввп —
      залежно від ринків, які цікавлять) — CPI, Core CPI, PCE Price Index,
      Fed Funds Rate, 10Y/2Y Treasury Yield, Unemployment Rate, Non-Farm
      Payrolls, Initial Jobless Claims, Real GDP, Retail Sales, Housing
      Starts, 30Y Mortgage Rate (macro/fred_adapter.py) + HICP, Deposit
      Facility Rate, Unemployment Rate для єврозони (macro/ecb_adapter.py)
- [ ] Дані компаній: звіти (earnings), базові фінансові метрики
- [ ] Новини: підключення джерела + фільтрація за релевантністю
- [ ] Календар релізів (коли виходять наступні дані) — це критично для
      Фази 3 (моніторинг)
- [ ] Нормалізація: усі джерела приводяться до єдиного внутрішнього формату

**Критерій завершення фази:** дані з кількох різних джерел стабільно
збираються за розкладом без ручного втручання.

## Фаза 2 — Аналіз і прогнози
- [ ] Формування "ринкових очікувань" для показника (звідки їх брати —
      окреме дослідження: консенсус-прогнози, або власна модель)
- [ ] Порівняння факту з очікуванням при виході нових даних
- [ ] Базовий механізм прогнозування (почати з простого — наприклад,
      трендові моделі — перш ніж ускладнювати)
- [ ] LLM-аналіз новин/звітів → структурований висновок (не вільний текст)

**Критерій завершення фази:** для одного показника агент може сказати
"вийшло X, очікувалось Y, це означає Z" одразу після релізу.

## Фаза 3 — Моніторинг і реакція
- [ ] Трекінг календаря релізів, тригер на "дані щойно вийшли"
- [ ] Автоматичне оновлення прогнозів після кожного релізу
- [ ] Сповіщення про суттєві відхилення від очікувань

## Фаза 4 — Звітність і списки активів
- [ ] Генерація короткого регулярного звіту (щоденний/щотижневий)
- [ ] Формування списку "цікавих активів" на основі накопиченого аналізу
- [ ] Формат видачі (email/dashboard/файл — вирішити окремо)

## Поза MVP (навмисно відкладено)
- Бектестинг стратегій
- Портфельна оптимізація
- Мультикористувацький режим / авторизація
- Реальні угоди/виконання ордерів (агент тільки аналізує, не торгує)

---
## Журнал сесій
(додавати короткий запис після кожної завершеної сесії — 2-3 речення:
що зроблено, що далі)

- 2026-08-29: Реалізовано скелет Фази 0 — db/schema.sql (raw_observations
  append-only + revision, sources, release_log), спільний інтерфейс
  адаптера (common/adapter.py), перший адаптер FRED (cpi/fed_funds_rate/
  unemployment_rate), запис у БД з автовизначенням ревізії (common/db.py),
  CLI run_collect.py, Telegram-сповіщення (reporting/telegram_notify.py),
  docker-compose.yml, 13 тестів на фікстурах — усі проходять.
- 2026-08-29: Перехід на повністю докеризований воркфлоу (Dockerfile +
  сервіс app, без venv на хості) і виправлення реальних багів,
  знайдених при живому запуску: (1) create_hypertable падала, бо
  UNIQUE(source, metric_id, observed_at, revision) не містив колонку
  партиціювання fetched_at — перейшли на партиціювання по observed_at;
  (2) через це INSERT INTO sources у кінці init-скрипта не виконувався,
  звідси ForeignKeyViolation — додано data-ingestion/apply_schema.py
  для явного ідемпотентного (пере)застосування схеми, незалежного від
  docker-entrypoint-initdb.d. Наскрізний прогін (run_collect.py →
  TimescaleDB → telegram_notify.py) підтверджено вживу. **Фаза 0
  завершена.** Далі: Фаза 1 — ширина збору даних (ще 2-4 макропоказники,
  перший адаптер companies/, перший адаптер news/).
- 2026-08-29 (сесія 2): Узгоджено принцип (закріплено в
  docs/decisions.md) — data-ingestion зберігає тільки сирі значення,
  без % змін і будь-яких розрахунків, це відповідальність analysis/
  (Фаза 2). Створено docs/metrics-catalog.md — повний робочий список
  показників для Фази 1+ (макро США/єврозона, компанії, ринок,
  крипто, новини, календар релізів), з позначками що вже зібрано /
  адаптер готовий / ще в плані.
- 2026-08-29 (сесія 3): Розширено macro/fred_adapter.py:METRICS трьома
  показниками з плану Фази 1 — Core CPI (`core_cpi` → `CPILFESL`),
  PCE Price Index (`pce_price_index` → `PCEPI`), Non-Farm Payrolls
  (`nonfarm_payrolls` → `PAYEMS`). Новий адаптер не знадобився —
  run_collect.py, тести (test_all_declared_metrics_have_series_id) і
  Docker-запуск підхоплюють нові metric_id автоматично. Синхронізовано
  METRIC_LABELS у reporting/telegram_notify.py та статуси в
  docs/metrics-catalog.md. Усі 13 тестів проходять.
- 2026-08-29 (сесія 3, продовження): Тим самим способом додано решту
  показників зі списку макро-США: 10Y/2Y Treasury Yield
  (`treasury_10y`/`treasury_2y` → `DGS10`/`DGS2` — база для спреду
  10Y-2Y, індикатор рецесії, сам спред рахуватиме analysis/ у Фазі 2),
  Initial Jobless Claims (`initial_jobless_claims` → `ICSA`), Real GDP
  (`real_gdp` → `GDPC1`), Retail Sales (`retail_sales` → `RSAFS`).
  Синхронізовано METRIC_LABELS і docs/metrics-catalog.md. Усі 13
  тестів проходять локально (юніт, на фікстурах — без реального
  виклику FRED API).
- 2026-08-29 (сесія 3, завершення US-макро): Додано останні два
  опційні показники — Housing Starts (`housing_starts` → `HOUST`),
  30Y Fixed Mortgage Rate (`mortgage_rate_30y` → `MORTGAGE30US`).
  Макро — США тепер повністю закрито: 13 показників у
  macro/fred_adapter.py:METRICS. Живий прогін усіх нових metric_id
  проти реального FRED API/TimescaleDB/Telegram підтверджено
  користувачем локально — усе ОК.
- 2026-08-29 (сесія 4): Перший новий адаптер поза FRED —
  `macro/ecb_adapter.py` (ECB Data Portal, колишній SDW). Формат
  `csvdata` замість SDMX-JSON (простіший normalize()). Три показники
  єврозони: `eurozone_hicp` (ICP/M.U2.N.000000.4.ANR, аналог CPI),
  `eurozone_deposit_rate` (FM/D.U2.EUR.4F.KR.DFR.LEV, аналог Fed Funds
  Rate), `eurozone_unemployment_rate` (STS/M.I8.S.UNEH.RTT000.4.000).
  API-ключ не потрібен (на відміну від FRED). Серії ідентифікуються
  парою flowRef+seriesKey (не одним series_id, як у FRED) —
  METRICS тут dict[str, tuple[str, str]]. run_collect.py узагальнено
  до реєстру ADAPTERS (fred/ecb) з автовизначенням джерела за
  metric_id; той самий підхід у reporting/telegram_notify.py
  (_METRIC_SOURCE) — виправлено баг: --source мав default="fred"
  жорстко, що зламало б sourced-запити для ecb-метрик. requests уже
  був у requirements.txt — нової залежності й ребілду образу не
  знадобилось. Додано docs/metrics-catalog.md розділ "Макро — Азія"
  (у плані, джерела по країнах ще не досліджені — Японія/e-Stat,BOJ;
  Китай/NBS,PBOC; Індія/MOSPI,RBI). Юніт-тести (7 нових для ECB) усі
  проходять локально на фікстурі `tests/fixtures/ecb_hicp_response.csv`
  — **живий прогін проти реального ECB API ще не робився**, потрібен
  наступний крок перед комітом (ECB не потребує .env-ключа, тож
  досить `docker compose exec app python data-ingestion/run_collect.py
  --metric eurozone_hicp`).
  **Наступна сесія починає звідси:** після підтвердження живого
  прогону ECB користувачем — Макро — Азія (спершу дослідження джерел
  по кожній країні окремо через web_search, як робилось для ECB, бо
  єдиного порталу з SDMX API для всієї Азії немає) або перший
  адаптер поза макро: `companies/` (SEC EDGAR) чи `news/` (GDELT).
- 2026-08-29 (сесія 4, виправлення після живого тесту): Живий прогін
  eurozone_hicp/eurozone_deposit_rate виявив дві помилки:
  1) забув додати рядок `('ecb', ...)` у seed `sources` у
     db/schema.sql — FK violation при INSERT у raw_observations.
     Виправлено (apply_schema.py безпечно перезапускати — ON CONFLICT
     DO NOTHING). **Урок на майбутнє: кожне нове джерело (не тільки
     новий metric_id) вимагає рядка в sources — інакше 1-й же live
     insert падає з ForeignKeyViolation, юніт-тести цього не ловлять,
     бо не торкаються БД.**
  2) `eurozone_unemployment_rate` мав неправильний series key —
     `STS.M.I8.S.UNEH.RTT000.4.000` (застарілий, датасет STS
     перейменовано/перенесено) повертав 404. Правильний, перевірений
     через web_search проти реальних цифр Eurostat (6.1-6.3% за
     2025-2026): `LFSI.M.U2.S.UNEHRT.TOTAL0.15_74.T` (dataflow LFSI —
     Labour Force Survey Indicators). **Урок: для ECB (на відміну від
     FRED, де series_id перевірявся по факту роботи) варто
     верифікувати кожен series key живим запитом (напр. web_fetch)
     ДО того, як віддавати користувачу на live-тест, а не покладатись
     на непрямі джерела (R-пакети/статті) без перевірки актуальності.**
  Обидві правки — тільки локальні дані/код, ще не підтверджені живим
  прогоном користувача. **Наступна сесія починає звідси:** дочекатись
  підтвердження (`apply_schema.py` + всі 3 eurozone_* metric_id), тоді
  Макро — Азія або companies/news.
- 2026-08-29 (сесія 4, підтверджено): Живий прогін після виправлень —
  `apply_schema.py` (sources отримав `ecb`), усі 20 тестів, і всі 3
  eurozone_* metric_id (hicp, deposit_rate, unemployment_rate) успішно
  записались у raw_observations. Макро — єврозона повністю закрито.
  **Наступна сесія починає звідси:** Макро — Азія (дослідження джерел
  по кожній країні окремо, немає єдиного SDMX-порталу як FRED/ECB —
  Японія/e-Stat,BOJ; Китай/NBS,PBOC; Індія/MOSPI,RBI) або перший
  адаптер поза макро: `companies/` (SEC EDGAR) чи `news/` (GDELT).
- 2026-08-30 (сесія 5): Користувач повідомив, що `eurozone_hicp`
  повертав застарілі дані (грудень 2025, при тому що реально
  опубліковано вже за липень 2026). Причина — не баг коду: ECB
  офіційно закрив датасет `ICP` 4 лютого 2026 (методологічна реформа
  Eurostat — COICOP v2, база 2025=100, Болгарія в складі єврозони з
  1 січня 2026) і замінив його новим датасетом `HICP` з іншим кодом
  інституції в ключі серії (`4` → `4D0`). Запит до старого flowRef не
  падав з помилкою — просто мовчки не отримував нових спостережень.
  Виправлено: `eurozone_hicp` тепер `HICP.M.U2.N.000000.4D0.ANR`
  (перевірено живим пошуком проти офіційних цифр Eurostat). Повний
  урок і чому "перевірено живим запитом раніше" цього разу не
  врятувало — docs/decisions.md, запис 2026-08-30.
  Також: (1) прибрано зайву дубльовану теку `analysis ` (з пробілом
  у назві, точна копія `analysis/CLAUDE.md`) — артефакт помилки в
  одному з попередніх архівів; (2) написано повний README.md з нуля
  (раніше — тільки заголовок): вимоги, покрокове розгортання на
  новому ПК/сервері, щоденні команди, перевірка БД напряму, повне
  перезбирання з `docker compose down -v` (з попередженням і
  командою для дампу перед видаленням volume), усунення проблем.
  Live-тест виправленого eurozone_hicp користувачем ще не підтверджено
  — заплановано разом із повним перезбиранням Docker (`down -v` +
  `up -d --build`), бо користувач підозрює проблему з застарілим
  станом Docker-БД (не підтверджено діагностикою, але це безпечна дія).
  **Наступна сесія починає звідси:** дочекатись підтвердження після
  перезбирання (усі 3 eurozone_* + US-метрики мають зʼявитись у БД
  заново), тоді Макро — Азія або companies/news.
- 2026-08-30 (сесія 6-7): Macro — Японія. Дослідив джерела: Policy
  Rate через Bank of Japan Time-Series API (без ключа) —
  `macro/boj_adapter.py` (metric_id `japan_policy_rate`, db=FM01,
  код STRDCLUCON). CPI через e-Stat (потрібен `ESTAT_APP_ID`).
  Користувач підтвердив живий прогін `japan_policy_rate` (5 записів
  у raw_observations). Користувач прямо вказав: Китай і Індію не
  розглядаємо взагалі (остаточне рішення, не тимчасове відкладення,
  зафіксовано в docs/decisions.md). Додано `usdjpy_fx_rate` для
  carry trade (FRED, DEXJPUS — свідомо не через BOJ FM08, щоб не
  тримати дві неперевірені структури відповіді одночасно).
  Користувач додав `ESTAT_APP_ID` у .env → написано
  `macro/estat_adapter.py` (metric_id `japan_cpi`, statsDataId
  0004052037, база 2025=100). На відміну від FRED/ECB/BOJ, e-Stat
  вимагає резолюції кодів area="全国"/cat01="総合" через метадані
  API (двоетапний fetch — деталі й причина в docs/decisions.md),
  бо ці коди ніде не задокументовані у зручному вигляді наперед.
  15 нових тестів на фікстурах (estat), 7 (boj) — усі проходять.
  **Живий тест e-Stat ще не робився** (мережа пісочниці Claude не
  має доступу до e-stat.go.jp) — `_resolve_area_and_cat01()` кине
  зрозумілу ValueError, якщо коди не знайдені, замість тихого
  провалу.
  Також додано `data-ingestion/collect_all.py` — перезбір усіх
  метрик за один запуск (потрібно було, бо БД спорожніла після
  `docker compose down -v` з сесії 5).
  **Наступна сесія починає звідси:** (1) `docker compose exec app
  python data-ingestion/apply_schema.py` (додасть джерела boj/estat
  у sources); (2) `docker compose exec app python
  data-ingestion/collect_all.py` — перезбере все одразу, включно з
  japan_cpi; (3) якщо `japan_cpi` впаде з ValueError від
  `_resolve_area_and_cat01` — надіслати текст помилки, виправлю
  адресно. Коли Японія повністю закрита — companies/ (SEC EDGAR) або
  news/ (GDELT), наступні за PLAN.md.
- 2026-09-13 (сесія 8): Перший живий прогін через Docker.
  `apply_schema.py` пройшов чисто. `collect_all.py`: усі 18
  US/eurozone/BOJ metric_id відпрацювали (0 нових — дані ті самі, що
  вже були в raw_observations). `japan_cpi` впав — але не з
  ValueError від `_resolve_area_and_cat01`, а з
  `NameResolutionError` на рівні DNS (`api.e-stat.go.jp` не
  резолвиться з контейнера). Hostname звірено як правильний за
  офіційною специфікацією e-Stat API v3.0 — код не чіпали.
  Користувач планує повне перезбирання Docker з нуля перед наступним
  прогоном (може саме усунути стейл DNS-кеш контейнера). Статус
  занесено в docs/decisions.md і docs/metrics-catalog.md (⚠️, не ✅).
  Питання "companies/ (SEC EDGAR) чи news/ (GDELT) першими" —
  користувачу задано, відповідь ще не отримана.
  **Наступна сесія починає звідси:** (1) після перезбирання Docker —
  повторити `collect_all.py`, перевірити чи `japan_cpi` пройшов; якщо
  ні — новий `getent hosts`/`nslookup` діагноз з контейнера і з
  хоста окремо, можливо потрібен явний `dns:` блок у
  docker-compose.yml; (2) отримати відповідь користувача, який
  адаптер поза макро йде першим — SEC EDGAR чи GDELT — і почати його
  дослідження й реалізацію за тим самим протоколом (клонувати репо,
  прочитати існуючі адаптери, ECB — структурний шаблон).
- 2026-09-13 (сесія 9): DNS-збій з сесії 8 виявився разовим і сам
  зник. `japan_cpi` пройшов live-прогін, але логи показали фальшиві
  ревізії — знайдено реальний баг у `_resolve_area_and_cat01()`
  (резолвила тільки 2 з 3 вимірів e-Stat таблиці, ігноруючи `tab`).
  Виправлено: узагальнено до `_resolve_dimensions()` (резолвить усі
  виміри, явний `ValueError` для нерезольваних), додано другий шар
  захисту — дедуплікація в `normalize()`. Нові тести на обидва
  сценарії. Усі 43 тести проходять. Деталі — docs/decisions.md
  (2026-09-13, "japan_cpi — знайдено і виправлено реальний баг").
  **Японія повністю закрита** (`japan_policy_rate` + `japan_cpi`,
  обидва з живим підтвердженням). Макро-блок Фази 1 (США, єврозона,
  Японія) закрито повністю; Китай/Індія — фінально поза планом.
- 2026-09-14 (сесія 10): Користувач обрав companies/ (SEC EDGAR)
  першим адаптером поза макро. Дослідив SEC EDGAR API (companyfacts/
  companyconcept, CIK через company_tickers.json, обов'язковий
  User-Agent — контакт користувача отримано: brokerdata2022@gmail.com,
  збережеться як SEC_EDGAR_USER_AGENT у .env). Під час обговорення
  seed-watchlist користувач зупинив хід думки: мета — не моніторити
  довільні тикери, а знаходити ~10 акцій-кандидатів для торгів за
  критеріями. **Рішення (docs/decisions.md, 2026-09-14): спершу
  скринінг, потім збір даних, а не навпаки.** Це вимагає джерела
  котирувань (ціна/обсяг/капіталізація) — офіційного безкоштовного
  джерела для акцій не існує (на відміну від макро). Спершу обрано
  Stooq, але користувач тим самим кроком запитав про альтернативи
  через брокерські API — **ревізовано в межах тієї самої сесії:
  Alpaca Markets замінює і Stooq, і окреме питання джерела universe-
  списку одним джерелом** (`GET /v2/assets` — весь tradable-лістинг
  NYSE/NASDAQ як universe, `/v2/stocks/bars`+`/snapshot` — ціна/обсяг,
  `/v1beta1/screener/stocks/movers`+`most-actives` — доступні, роль у
  воронці ще не вирішена). Деталі й відкинуті альтернативи (Alpha
  Vantage — 25 запитів/день, замало; Twelve Data — запасний варіант)
  — docs/decisions.md, два записи 2026-09-14 (початковий + ревізія).
  Створено `docs/screening-criteria.md`: трирівнева воронка (Tier A —
  ліквідність/Alpaca, Tier B — фундаментальна якість/EDGAR, Tier C —
  valuation/обидва джерела), пороги начорно, потребують калібрування
  на реальних даних. Архітектурний наслідок для `companies/`:
  `METRICS` буде словником **концептів** (revenue/eps_diluted/
  net_income/shares_outstanding → us-gaap tag), тикер — параметр
  виклику, а не хардкод (на відміну від фіксованих METRICS у FRED/
  ECB/BOJ/e-Stat) — код `companies/` ще не писався. Користувач
  зареєструє Alpaca paper-акаунт і додасть `APCA_API_KEY_ID`/
  `APCA_API_SECRET_KEY` у `.env`.
  **Наступна сесія починає звідси:** (1) узгодити з користувачем
  відкриті питання в `docs/screening-criteria.md` (роль Alpaca
  movers/most-actives у воронці; пороги Tier A-C; частота
  ребалансування; формула score); (2) після узгодження і отримання
  `APCA_API_KEY_ID`/`APCA_API_SECRET_KEY` від користувача —
  `quotes/alpaca_adapter.py` (новий, поза `macro/`; категорія джерела
  `quotes` — додати в `db/schema.sql` `sources`); (3) прогін скринера
  на universe, отримати конкретний список ~10 тикерів; (4) тоді
  `companies/` (SEC EDGAR) — генерична реалізація з `METRICS`-
  словником концептів, зібрати дані саме по цих тикерах.
- 2026-09-20 (сесія 11): Alpaca відкочено. Користувач повідомив, що
  `alpaca.markets` і документація взагалі не відповідають (не
  API-помилка — сайт не завантажується). Дослідив: Alpaca офіційно має
  недокументовані публічно географічні обмеження (довідкова сторінка
  "Countries Alpaca is available" — писати в support, щоб дізнатись
  про свою країну). Точна причина (гео-блок Alpaca чи локальна мережа)
  не підтверджена тестом з іншої мережі — але зважаючи на
  невизначеність і те, що Alpaca взагалі вимагає реєстрації реального
  акаунту, продовжувати недоцільно. **Фінальне рішення (docs/decisions.md,
  2026-09-14/20):** Stooq повертається для котирувань (без ключа/
  акаунту, як і планувалось спочатку). Для universe — замість
  `/v2/assets` (Alpaca) чи scraping Wikipedia (розглядалось ще до
  Alpaca) — `https://www.sec.gov/files/company_tickers_exchange.json`:
  офіційний SEC-файл (`cik`/`name`/`ticker`/`exchange`), той самий
  хост, що вже потрібен для `companies/`. Це виявилось краще за обидва
  попередні варіанти — офіційніше за Alpaca, без scraping, без
  акаунтів для universe чи котирувань узагалі. `docs/screening-criteria.md`
  оновлено під ці джерела; відкрите питання про роль Alpaca movers/
  most-actives знято (джерела більше нема).
  **Наступна сесія починає звідси:** узгодити з користувачем 4 відкриті
  питання в `docs/screening-criteria.md` (пороги Tier A-C; частота
  ребалансування; формула score; орієнтовний обсяг запитів до Stooq на
  повний прогін universe — кілька тисяч тикерів). Після узгодження —
  `quotes/stooq_adapter.py` (новий, поза `macro/`; категорія джерела
  `quotes` — додати в `db/schema.sql` `sources`), потім прогін скринера
  на universe → конкретний список ~10 тикерів → тоді `companies/`
  (SEC EDGAR), генерична реалізація з `METRICS`-словником концептів.
- 2026-09-20 (сесія 12): Закрито всі 4 відкритих питання скринінгу.
  (1) Пороги Tier A-C визначено самостійно на професійному рівні (не
  чернетка навмання): Tier A — ціна >$10, капіталізація >$10 млрд
  (large-cap), обсяг >$10 млн/день у доларах; Tier B — прибутковість,
  revenue growth YoY >10%, EPS growth YoY >15%, дилюція <3% YoY, новий
  критерій боргу (Liabilities/Assets <0.6); Tier C — P/E 10-35, P/S <8,
  новий критерій PEG <2. (2) Ребалансування — синхронно з 10-Q
  (щокварталу). (3) Формула composite score визначена: зважена сума
  процентильних рангів (0.35 revenue growth + 0.30 EPS growth + 0.20
  інверсія P/E + 0.15 доларовий обсяг) серед кандидатів, що пройшли
  hard-фільтри. (4) Universe звужено до **S&P 500** (~500 тикерів,
  джерело — `datasets/s-and-p-500-companies` на GitHub) замість повного
  SEC-лістингу (~8000+) — і "не сканувати весь мотлох", і менше
  запитів на Stooq за прогін. Додано окремий механізм: акції з новин
  (майбутній `news/`) можуть потрапити в список поза плановим
  прогоном, але тільки після проходження Tier B — новина сама по собі
  не підстава, лише тригер перевірки. `companies/` під це вже
  спроєктований (тикер — параметр виклику). Усе — в
  `docs/screening-criteria.md` (переписано načisto, більше не
  чернетка) і `docs/decisions.md`.
  **Наступна сесія починає звідси:** `quotes/stooq_adapter.py` (новий,
  поза `macro/`; категорія джерела `quotes` — додати в
  `db/schema.sql` `sources`; дослідити формат Stooq CSV і практичні
  ліміти на ~500 тикерів S&P 500 живими запитами, як робилось для
  кожного попереднього джерела). Далі — прогін скринера на universe →
  конкретний список ~10 тикерів → `companies/` (SEC EDGAR) генерична
  реалізація з `METRICS`-словником концептів для цих тикерів.
- 2026-09-20 (сесія 13): Написано `quotes/stooq_adapter.py` —
  `FIELDS` (close/volume) замість METRICS, тикер — параметр
  конструктора, `metric_id` = `{ticker}_{field}`. `run_collect.py`
  узагальнено: новий `--ticker` (взаємовиключний з `--metric`) і
  `id_kwarg` у реєстрі `ADAPTERS` — той самий механізм перевикористає
  `companies/` пізніше. `db/schema.sql` — додано джерело `stooq`
  (category `quotes`). 10 нових тестів на фікстурі, разом 53/53
  проходять локально. `docs/metrics-catalog.md` — нова секція Quotes.
  Деталі — `docs/decisions.md`, 2026-09-20.
  **Не зроблено:** живий прогін проти реального Stooq — `stooq.com`
  не в дозволених доменах пісочниці Claude, формат CSV спирається на
  задокументовану поведінку з кількох джерел, не на власну перевірку.
  **Наступна сесія починає звідси:** користувач розпаковує код,
  запускає `docker compose exec app python data-ingestion/run_collect.py
  --source stooq --ticker AAPL` (спершу `apply_schema.py`, якщо ще не
  застосовано новий `sources`-рядок). Якщо формат CSV відрізняється
  від очікуваного — `normalize()` кине `ValueError` з першими 200
  символами сирої відповіді в тексті помилки, за тим самим протоколом,
  що і для e-Stat/ECB: надіслати текст помилки, виправлю адресно.
  Після підтвердження — прогін скринера на весь S&P 500 (потрібен
  окремий скрипт поза `run_collect.py`, який зараз забирає по одному
  тикеру за раз — `run_collect.py` для цього не призначений, це
  дебаг-інструмент за задумом) → список ~10 тикерів → `companies/`
  (SEC EDGAR).
- 2026-09-20 (сесія 14): Живий тест `--source stooq --ticker AAPL`
  дав 404. Замість запитати користувача, що реально повертає сайт, я
  пішов шукати причину у вебпошуку, знайшов два джерела про нібито
  обов'язковий `apikey` з квітня 2026, і переписав адаптер під це —
  без жодної живої перевірки. Користувач вказав на помилку прямим
  текстом. Користувач надав реальний CSV (`aapl_us_d.csv`,
  10589 днів AAPL, 1984-2026) і робочий URL
  (`?s=aapl.us&i=d&c=1`) — **жодного ключа не треба**, і виявилась
  друга, важливіша річ, якої вебпошук показати не міг: **роздільник
  CSV — `;`, не `,`** (Stooq — польський сервіс). Перша версія з
  комою мовчки зламала б парсинг. Обидва фікси застосовано,
  apikey-вимогу відкочено повністю. Перевірено наскрізно на всьому
  реальному файлі: 21178 записів, 0 дублікатів, 0 помилок парсингу.
  Тести (фікстура тепер — фрагмент реального файлу) — 53/53. Повний
  запис і чесний розбір помилки — `docs/decisions.md`, 2026-09-20
  ("помилковий висновок про apikey").
  **Наступна сесія починає звідси:** користувач розпаковує код,
  запускає `docker compose exec app python data-ingestion/run_collect.py
  --source stooq --ticker AAPL` — цього разу з реальним підтвердженням
  формату, а не гіпотезою. Якщо запис у БД пройде успішно — прогін
  скринера на весь S&P 500 (окремий скрипт поза `run_collect.py`,
  той для одного тикера за раз) → список ~10 тикерів → `companies/`
  (SEC EDGAR).
- 2026-09-20 (сесія 15): Той самий URL з попередньої сесії далі
  давав 404 з контейнера, попри те що спрацював у браузері. Спільна
  діагностика (default vs. browser User-Agent) показала: Stooq
  ставить JS-based бот-перевірку — `requests` не може її пройти
  в принципі, без headless-браузера. **Stooq остаточно відкинуто**
  (другий "без реєстрації" варіант поспіль, що виявився нежиттєздатним
  — перший був Alpaca, гео-блок). Замість третього раунду "пошук →
  здогадка → код", попросив користувача самому зареєструватись і
  перевірити **Twelve Data** (запасний варіант ще з першого запису)
  живим запитом до написання коду. Користувач це зробив — 200,
  реальні дані AAPL. `quotes/stooq_adapter.py` видалено,
  `quotes/twelvedata_adapter.py` написано під підтверджений формат
  (JSON, значення рядками). `run_collect.py`, `.env.example`,
  `db/schema.sql`, `docs/metrics-catalog.md`, `docs/screening-criteria.md`
  оновлено з Stooq на Twelve Data. Ліміти free tier: ~8 запитів/хв,
  800/добу — прогін скринера на весь S&P 500 вкладається. Повний
  запис — `docs/decisions.md`, 2026-09-20 ("Stooq відкинуто —
  бот-захист, Twelve Data — джерело котирувань").
  **Наступна сесія починає звідси:** користувач розпаковує код,
  запускає (реальний ключ Twelve Data вже підтверджено робочим):
  ```
  docker compose exec app python data-ingestion/run_collect.py \
    --source twelvedata --ticker AAPL
  ```
  Якщо запис у БД пройде успішно — прогін скринера на весь S&P 500
  (окремий скрипт поза `run_collect.py`, той для одного тикера за
  раз, потребує `TWELVEDATA_API_KEY` в `.env`) → список ~10 тикерів
  → `companies/` (SEC EDGAR).
- 2026-09-20 (сесія 16): Живий тест `--source twelvedata --ticker
  AAPL` користувача пройшов (10 записів у БД). Написано
  `data-ingestion/collect_universe.py` — прогін по всьому S&P 500
  (503 тикери), пауза 8с між запитами (ліміт Twelve Data ~8/хв),
  ~65 хв на повний прогін. Список тикерів —
  `constituents.csv` (github.com/datasets/s-and-p-500-companies),
  живо перевірено (не здогадка): реальні колонки ширші за старі
  описи датасету (`Symbol,Security,GICS Sector,GICS Sub-Industry,
  Headquarters Location,Date added,CIK,Founded`), 503 рядки, 2
  тикери з крапкою (`BRK.B`, `BF.B`) — підтверджено, що проходять
  без спецобробки. Бонус: файл вже містить `CIK` — знадобиться для
  `companies/` (SEC EDGAR) без окремого тикер→CIK лукапу.
  **Наступна сесія починає звідси:** користувач розпаковує код,
  запускає `docker compose exec app python data-ingestion/collect_universe.py
  --limit 10` (тестовий прогін на 10 тикерах, ~80с) перш ніж
  повний. Якщо ок — повний прогін (`--limit` без значення, ~65 хв).
  Далі: сам скринер (Tier A на зібраних Twelve Data цінах/обсягах) —
  це вже `analysis/`, перший код там. Паралельно/після — `companies/`
  (SEC EDGAR) для тикерів, що пройдуть Tier A.
- 2026-09-20 (сесія 17): Поки йшов прогін `collect_universe.py`,
  користувач задав напрямок для `news/`: окремий watchlist активів
  поза акціями S&P 500 — метали (золото, срібло), енергоносії (нафта,
  газ), кава, валютні пари (EUR/USD, USD/JPY), крипторинок загалом.
  Задокументовано `docs/watchlist.md` — пріоритет 1 (названо явно) і
  пріоритет 2 (загальні, ще не визначено — наступний крок).
  Нез'ясовано: конкретні крипто-монети (я припустив BTC+ETH як
  дефолт, не підтверджено), WTI чи Brent для нафти.
  **Наступна сесія починає звідси:** (1) якщо `collect_universe.py`
  завершився — перевірити результат (`SELECT COUNT(DISTINCT
  metric_id) FROM raw_observations WHERE source='twelvedata'`, має
  бути ~1006 — 503 тикери × 2 поля), почати `analysis/` (Tier A
  скринінг на зібраних даних). (2) уточнити з користувачем відкриті
  пункти `docs/watchlist.md` (крипто-монети, WTI/Brent, пріоритет 2),
  тоді обирати джерела даних під commodities/forex/crypto — це ще не
  робилось, кожне джерело досліджувати за тим самим протоколом
  (жива перевірка перед кодом, не здогадка з пошуку).
- 2026-09-20 (сесія 18): Поки йшов `collect_universe.py`, написано
  `companies/sec_edgar_adapter.py` — генерична реалізація за раніше
  узгодженою схемою (`METRICS`-словник концептів, тикер — параметр,
  CIK через `company_tickers.json`). Наперед застосовано захист від
  класу багу, знайденого раніше в `japan_cpi` (мікс кількох вимірів
  під одним metric_id): XBRL duration-концепти фільтруються за
  довжиною періоду, щоб не змішати квартальні й кумулятивні значення.
  `sec.gov` немає в дозволених доменах пісочниці Claude — формат
  відповіді підтверджено кількома незалежними джерелами, але НЕ
  живим запитом, чесно позначено в коді й docs. 11 нових тестів,
  разом 63/63. `run_collect.py`, `.env.example`
  (`SEC_EDGAR_USER_AGENT`), `db/schema.sql`, `docs/metrics-catalog.md`
  оновлено. Деталі — `docs/decisions.md`, 2026-09-20.
  **Наступна сесія починає звідси:** (1) перевірити результат
  `collect_universe.py`, якщо ще не перевірено; (2) живий тест
  `docker compose exec app python data-ingestion/run_collect.py
  --source sec_edgar --ticker AAPL` — перша реальна перевірка формату
  companyconcept; якщо щось розходиться з фікстурами — `ValueError`
  чи вміст відповіді підкажуть, що саме; (3) далі — `analysis/`
  (перший код там): Tier A на зібраних Twelve Data даних, потім
  Tier B на SEC EDGAR для тих, хто пройшов.
- 2026-09-21 (сесія 19): `collect_universe.py` підтверджено завершеним
  — 982 з очікуваних 1006 metric_id (503×2), 68734 рядків. 24
  бракує — не копали (12 тикерів з якоюсь помилкою), не блокує
  наступний крок. Живий тест `sec_edgar --ticker AAPL` пройшов (739
  записів), але лог показав "ping-pong" ревізії — SEC для того
  самого кварталу інколи дає два записи з різним val (спліт акцій
  для EPS; дрібні розбіжності між поданнями для assets/liabilities).
  Виправлено: дедуплікація по `observed_at`, береться запис з
  найпізнішим `filed`. Той самий клас рішення, що фікс `japan_cpi`
  (сесія 9) — інша конкретна причина. Новий тест на реальних
  значеннях з логу (`13.81`→`1.97` за 2012-12-29). Разом 64/64.
  Користувач також запитав про архітектуру БД (одна таблиця
  `raw_observations` — це TimescaleDB hypertable, свідомий вибір, не
  спрощення) — додано 2 SQL-view (`v_current_values`,
  `v_observations_latest_revision`) у `db/schema.sql` для читабельного
  перегляду, сама append-only таблиця не змінена.
  **Наступна сесія починає звідси:** користувач розпаковує код,
  `apply_schema.py` (додасть views), тоді
  `run_collect.py --source sec_edgar --ticker AAPL` ще раз — перевірити,
  що дублі-ревізії зникли (`SELECT * FROM v_current_values WHERE
  metric_id LIKE 'aapl_%'`). Якщо ок — перший код `analysis/`: Tier A
  на зібраних Twelve Data даних (982 metric_id вже в БД).
- 2026-09-23 (сесія 20): Користувач підтвердив дедуплікацію (дублі
  зникли, `v_current_values` показав 8 чистих рядків для AAPL).
  Перш ніж Tier A — закрив прогалину: `companies/` (SEC EDGAR) досі
  було прогнано тільки на AAPL, а Tier A потребує `shares_outstanding`
  для капіталізації по ВСЬОМУ S&P 500. Написано
  `collect_companies_universe.py` — CIK з `constituents.csv` (без
  окремого лукапу на кожен тикер), `SecEdgarAdapter.fetch()` тепер
  пацує запити (0.15с, SEC ліміт 10/сек) — додано наперед, до
  масового прогону. Далі — перший код `analysis/`:
  `analysis/screening/tier_a.py`, критерії з `docs/screening-criteria.md`
  (ціна >$10, капіталізація >$10 млрд, сер. $ обсяг 63 дні >$10 млн).
  Логіка порогів — чиста функція `passes_tier_a()`, 6 тестів без БД;
  сам SQL-шар не тестований (TimescaleDB немає в пісочниці Claude).
  Разом data-ingestion 64/64, analysis 6/6.
  **Наступна сесія починає звідси:** користувач розпаковує код,
  запускає (~7-8 хв на 503×6 запитів із паузою):
  ```
  docker compose exec app python data-ingestion/collect_companies_universe.py
  ```
  Тоді перший прогін скринера:
  ```
  docker compose exec app python analysis/screening/tier_a.py
  ```
  Це перша жива перевірка і масового SEC EDGAR збору, і самого SQL у
  `tier_a.py` — обидва не тестовані живо в цій сесії. Якщо результат
  виглядає дивним (замало/забагато пройшло) — конкретні числа
  підкажуть, що саме коригувати (пороги чи запит). Після Tier A —
  Tier B (SEC EDGAR фундаментал) на тих, хто пройшов.
- 2026-09-24 (сесія 21): Живий прогін підтвердив і масовий SEC EDGAR
  збір (500 успішно, 3 з помилкою — BSX/FLEX/V, не копали), і Tier A
  (після фіксу psycopg2 `%%`-екранування — `IndexError: tuple index
  out of range` через параметризований запит з нелекранованим `%` в
  LIKE): 365 з 491 пройшли. 110 без `shares_outstanding` — перевірено
  напряму (SEC повертає 200, але `units.shares={}` порожній) — реальний
  пробіл джерела (компанії з кількома класами акцій?), не баг,
  занотовано, не зараз фіксити. Перед Tier B — превентивний фікс:
  `aapl_revenue` зупинявся на 2018 (відомий пробіл, задокументований
  ще 2026-09-20, матеріалізувався). `METRICS` тепер підтримує кілька
  тегів на концепт, `revenue` фетчить і `Revenues`, і
  `RevenueFromContractWithCustomerExcludingAssessedTax`, зливає через
  ту саму дедуплікацію по даті, що і спліт-дублікати. 65/65
  (data-ingestion). Написано `analysis/screening/tier_b.py` —
  чиста функція `passes_tier_b()` (profitability, revenue/EPS YoY,
  дилюція, L/A), 14 нових тестів без БД, analysis/ разом 20/20.
  **Наступна сесія починає звідси:** користувач розпаковує код,
  `docker compose exec app python data-ingestion/collect_companies_universe.py`
  ще раз (підхопить новий тег revenue для тих, у кого стара позначка
  застаріла), тоді:
  ```
  docker compose exec app python analysis/screening/tier_b.py
  ```
  Перша жива перевірка SQL у `tier_b.py` і самого YoY-підбору пари
  "рік тому" на реальних датах звітності. Якщо результат виглядає
  дивним — деталі (`revenue_yoy`/`eps_yoy`/etc. в `details`) підкажуть
  де саме. Після Tier B — Tier C (valuation, P/E/P/S/PEG), потім
  composite score → фінальний топ-10.
- 2026-09-25 (сесія 22): Tier C (`analysis/screening/tier_c.py`) —
  valuation-фільтр (P/E, P/S, PEG), TTM з 4 останніх кварталів, PEG
  переиспользує `eps_yoy` з Tier B. 19 тестів, живий прогін: 16/35
  пройшли. Composite score (`analysis/screening/composite_score.py`) —
  фінальне ранжування пройшли-Tier-C тикерів за формулою з
  `docs/screening-criteria.md`, 7 тестів `percentile_ranks()`. Разом
  analysis/ — 46/46. Уся воронка скринінгу (Tier A → B → C → ранжування)
  реалізована й живо підтверджена.
  **Далі:** відкриті другорядні питання (watchlist.md пріоритет 2,
  крипто-монети BTC/ETH, WTI vs Brent, shares_outstanding пробіл
  ~63-110 компаній) — не блокуючі. Наступний змістовний крок поза
  скринінгом: news/ (GDELT) або monitoring/ (календар релізів,
  Фаза 1 і Фаза 3 з PLAN.md).
