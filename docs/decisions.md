# Журнал рішень (ADR-лайт)

Формат кожного запису: дата, що вирішили, чому, які альтернативи відкинули.
Мета — щоб через 3 місяці (або нова сесія Claude) не довелось заново
переосмислювати "а чому ми взагалі так зробили".

### 2026-08-30: "Живий запит повернув 200 OK" ≠ "series key досі актуальний" — офіційні статдатасети іноді закриваються без помилки
**Рішення:** мало перевірити series key одним живим запитом перед
здачею користувачу (див. запис нижче, 2026-08-29). Додатково: якщо
отримане значення застаріле більш ніж на один звітний період — це
сигнал перевірити, чи датасет/dataflow не було замінено/закрито,
а не проблема з `lastNObservations`/логікою фільтрації.
**Чому:** `eurozone_hicp` через flowRef `ICP` (перевірений живим
запитом і з правильною структурою CSV) стабільно повертав 200 OK і
коректно розпарсені записи — але дані завмерли на грудні 2025.
Причина: ECB офіційно ЗАКРИВ датасет `ICP` 4 лютого 2026 (методологічна
реформа Eurostat — COICOP v2, нова база 2025=100, склад єврозони з
Болгарією) і замінив його новим датасетом `HICP` з іншим кодом
інституції-джерела (`4` → `4D0`) у ключі серії. Запит до старого
flowRef не повертав помилку — просто мовчки повертав історичні дані
без нових спостережень, бо серія технічно існує, просто більше не
оновлюється. Виправлено: `eurozone_hicp` тепер `HICP.M.U2.N.000000.4D0.ANR`.
**Наслідок для процесу:** "перевірено живим запитом" (попередній
запис) страхує від помилки в ключі на момент перевірки, але не від
майбутнього/недавнього закриття датасету джерелом. Для показників,
де в нормалізованих записах observed_at суттєво відстає від очікуваної
дати останнього релізу (звірити із загальновідомим графіком публікацій
або пошуком), варто одразу підозрювати заміну датасету, а не
дебажити код адаптера.

### 2026-08-29: Новий адаптер = новий рядок у sources + перевірка кожного series key живим запитом
**Рішення:** при додаванні адаптера для нового джерела (не нового
metric_id у вже існуючому джерелі) обов'язково: 1) додати рядок у
seed `INSERT INTO sources` у db/schema.sql; 2) перевірити КОЖЕН
series key/series id живим запитом (web_fetch або аналог) до того,
як віддавати на live-тест користувачу — не покладатись на непрямі
джерела (R-пакети, статті, старі API-довідники) без верифікації, що
структура серій не застаріла.
**Чому:** обидва пункти виявились реальними, а не гіпотетичними
ризиками при додаванні ecb_adapter.py (Фаза 1, сесія 4). (1) Юніт-тести
на фікстурах не торкаються БД і тому не ловлять відсутність джерела в
`sources` — перший live insert падав з `ForeignKeyViolation`, і
з'ясувалось це аж на машині користувача. (2) Series key для
`eurozone_unemployment_rate`, підібраний за старим датасетом `STS`,
повертав 404 — датасет був перейменований/перенесений у `LFSI` без
явного попередження в непрямих джерелах, звідки бралась структура
ключа.
**Відкинута альтернатива:** довіряти юніт-тестам на фікстурах як
достатньому доказу готовності до live-тесту — відкинуто, бо вони за
конструкцією (навмисно, щоб не залежати від мережі/БД у тестах)
перевіряють тільки логіку normalize(), а не існування джерела в БД чи
актуальність зовнішнього API.

### 2026-08-29: raw_observations зберігає тільки сирі значення, не % зміни
**Рішення:** data-ingestion/ ніколи не рахує й не зберігає похідні
величини (MoM/YoY % зміна, тренди, порівняння факт/консенсус) —
тільки сире значення показника, яке віддає джерело. Розрахунки
живуть у analysis/ і рахуються з сирих значень на льоту або в
окремих похідних таблицях (деталі — коли дійдемо до Фази 2).
**Чому:** з сирого індексу (напр. CPI = 314.121) можна порахувати
будь-яку зміну (MoM, YoY, QoQ, зміна від попередньої ревізії) — не
треба наперед вирішувати, яка саме знадобиться. Якщо зберігати
одразу готовий %, втрачаємо гнучкість і, що важливіше, ламаємо
коректну обробку ревізій (коли джерело переглядає минуле значення
заднім числом — % зміна "з коробки" стає неоднозначною: від якої
версії минулого значення її рахували?).
**Відкинута альтернатива:** тягнути з FRED вже готові % через
параметр `units=pch/pc1` як окремі metric_id. Відкинуто на цьому
етапі — дублює дані, прив'язує нас до методології розрахунку FRED
замість власної, і суперечить принципу "джерело даних не інтерпретує".
Можна повернутись до цього як до додаткової звірки в Фазі 2.
Повний список показників для Фази 1+ — docs/metrics-catalog.md.

### 2026-08-27: Стек — Python + PostgreSQL/TimescaleDB
**Рішення:** Python як мова бекенду; PostgreSQL з розширенням
TimescaleDB як єдина база даних (замість окремих time-series + реляційної
СУБД).
**Чому:** один інструмент для обох типів даних (ціни/показники як
часові ряди, метадані/звіти як реляційні дані) — менше операційної
складності, ніж підтримка двох окремих СУБД. Обидва компоненти
безкоштовні й з відкритим кодом.

### 2026-08-27: Джерела даних — пріоритет безкоштовних першоджерел
**Рішення:** повний список у data-ingestion/CLAUDE.md. Ключові вибори:
FRED/ECB SDW для макро, SEC EDGAR для звітів компаній, yfinance для
цін (акції/форекс/товари), CoinGecko + Binance public API для крипти,
GDELT + офіційні RSS для новин.
**Чому:** максимальне покриття безкоштовно, з пріоритетом на офіційні
першоджерела там, де вони існують (FRED, SEC EDGAR, ECB, Binance).
Там, де офіційного безкоштовного API немає (ціни акцій/форекс/товарів
у реальному часі) — свідомо йдемо на компроміс через yfinance, з
явним застереженням про неофіційність і вимогою ізолювати це за
адаптером.
**Відкинуті альтернативи:** Alpha Vantage як основне джерело цін
акцій — відкинуто через занадто обмежений безкоштовний рівень (25
запитів/день), непридатний навіть для помірного покриття ринку.

### 2026-08-27: Календар релізів з рівнем впливу — власна розмітка
**Рішення:** дати релізів беремо з офіційних графіків публікацій
(BLS/BEA/Fed/ECB), а рівень впливу (High/Medium/Low) підтримуємо як
власний статичний список типів релізів, а не з зовнішнього API.
**Чому:** офіційного безкоштовного API з готовою розміткою впливу не
існує — найпоширеніші джерела такої розмітки (напр. ForexFactory)
неофіційні. Розмітка "що є high-impact" — загальновідома і рідко
змінюється (NFP, CPI, FOMC, GDP, PCE), тому підтримувати її вручну —
прийнятний компроміс.

### 2026-08-29: Виправлення — partition column для hypertable — observed_at, не fetched_at
**Рішення:** raw_observations партиціонується (TimescaleDB hypertable) по
`observed_at`, а не по `fetched_at`, як було зроблено спочатку.
**Чому:** перша спроба (partition по fetched_at) впала з реальною помилкою
під час запуску: `create_hypertable()` вимагає, щоб УСІ unique-обмеження
таблиці (не тільки PRIMARY KEY) включали колонку партиціювання. Наш
`UNIQUE (source, metric_id, observed_at, revision)` не містив fetched_at,
і hypertable не створювався — весь init-скрипт після цього кроку
переривався, включно з `INSERT INTO sources`, що спричиняло
ForeignKeyViolation при першому запуску run_collect.py. observed_at і
так частина цього UNIQUE-обмеження, тож партиціювання по ньому
розв'язує проблему без штучного розширення ключа. Додатковий бонус:
типові запити analysis/ читають "дані за період X" (observed_at), не
"коли ми дізнались" (fetched_at) — тож це й ефективніше.
**Наслідок для існуючих локальних БД:** якщо volume вже піднімався зі
старою схемою (і впав на цьому кроці), треба `docker compose down -v`
і підняти заново — таблиця не встигла отримати жодних валідних даних
(усі insert'и падали на FK через незаповнену sources), тож втрати
історії немає.

### 2026-08-29: Revision обчислює шар збереження, не адаптер
**Рішення:** адаптери (напр. FredAdapter) завжди повертають
NormalizedRecord з `revision=None`. Фактичний номер ревізії визначає
`common/db.py:decide_revision()` під час запису — порівнянням нового
значення з останнім уже збереженим для того самого
(source, metric_id, observed_at).
**Чому:** data-ingestion/CLAUDE.md вимагає, щоб адаптер повертав
`revision` як частину нормалізованого запису, але визначити ревізію
можна лише знаючи історію — а адаптер за архітектурою (docs/architecture.md)
не повинен звертатись до сховища. Винесення рішення "яка це ревізія"
в окрему чисту функцію (без SQL) дозволяє покрити логіку тестами без
реальної БД і лишає адаптер простим: він не знає нічого, крім самого зовнішнього API.
**Відкинуті альтернативи:** давати адаптеру доступ до БД для
самостійного визначення ревізії — відкинуто, бо порушує розділення
"адаптер тільки збирає" і ускладнює тестування адаптерів (довелось би
мокати БД в кожному тесті адаптера, а не лише в тестах db.py).

### 2026-08-30: Китай і Індію остаточно не розглядаємо (рішення користувача, не тимчасове відкладення)
**Рішення:** Макро — Азія обмежується Японією. Китай (NBS/PBOC) і
Індія (RBI/MOSPI) виключені з плану остаточно за прямою вказівкою
користувача — не "відкладено до появи офіційного API", а "не будемо
їх розглядати" в принципі.
**Чому:** жодне з двох джерел не має задокументованого публічного
REST API — `data.stats.gov.cn/easyquery.htm` і
`dbie.rbihub.in`/`data.rbi.org.in` розраховані на ручний
перегляд/Excel-експорт, не на програмний доступ. Це саме по собі
вже було підставою не братись за них зараз; пряма вказівка
користувача робить це остаточним рішенням проєкту.
**Наслідок для документації:** `docs/metrics-catalog.md` більше не
згадує Китай/Індію як заплановані — секція перейменована на
"Макро — Японія".

### 2026-08-30: Японія CPI — теж пройшла базову ревізію 2026 року, як і ECB HICP
**Рішення:** для e-Stat CPI Японії використовувати `statsDataId
0004052037` (2025 рік = 100, чинний з 21.08.2026), а не
`0003427113` (2020=100, попередній базовий рік).
**Чому:** Statistics Bureau of Japan (як і Eurostat для HICP)
провела чергову базову ревізію ІСЦ, новий базовий/ваговий рік 2025.
Старий `statsDataId` не видає помилку, просто не отримує нових
спостережень після перехідного періоду (продовжує оновлюватись лише
до грудня 2026) — той самий "тихий" режим відмови, що й `ICP`→`HICP`
в ECB. Урок звідти застосовано ще до написання коду цього разу.

### 2026-08-30: USD/JPY (carry trade) — обрано FRED (DEXJPUS), не BOJ FM08
**Рішення:** курс USD/JPY для carry trade збирається через
`macro/fred_adapter.py` (metric_id `usdjpy_fx_rate`, FRED series
`DEXJPUS`), а не через BOJ (db=FM08).
**Чому:** `DEXJPUS` — щоденна серія релізу Fed H.10, того самого
типу джерела, що й уже перевірені `fed_funds_rate`/`treasury_10y`
(не застаріла OECD MEI серія). Головна причина вибору саме FRED, а
не BOJ FM08 — уникнути другої неперевіреної структури відповіді
поряд із вже наявною для BOJ FM01 (japan_policy_rate); FRED-шлях уже
100% перевірений живими даними на інших метриках.

### 2026-08-30: e-Stat — коди area/cat01 резолвляться в рантаймі, не хардкодяться
**Рішення:** `macro/estat_adapter.py` робить два мережеві виклики на
кожен `collect()`: (1) невеликий запит із `metaGetFlg=Y&limit=1`,
щоб отримати `CLASS_INF` і знайти код area, чия назва — "全国"
(національний), і код cat01, чия назва — "総合" (усі товари); (2)
основний запит уже з `cdArea`/`cdCat01`, знайденими на кроці 1.
**Чому:** на відміну від FRED (один string ID) і ECB/BOJ (пара
ідентифікаторів, задокументована для потрібного ряду наперед), у
e-Stat потрібний зріз визначається кодами класифікації, які
публікуються тільки як додаток-таблиця до кожної статистичної
таблиці окремо (не в єдиному довіднику), і мережа пісочниці Claude
не мала доступу до e-stat.go.jp, щоб дістати ці коди заздалегідь.
Резолюція за людинозрозумілою назвою ("全国"/"総合") замість
хардкоду коду — стійкіша до того, що коди можуть відрізнятись між
`statsDataId` (напр. між 2020- і 2025-базовими таблицями).
**Відкрите питання:** сам живий виклик до e-stat.go.jp ще не
підтверджено (ключ ESTAT_APP_ID додано користувачем, але мережевий
запит з пісочниці Claude недоступний) — перший `collect_all.py`/
`run_collect.py --metric japan_cpi` через Docker або підтвердить
підхід, або поверне зрозумілу помилку від `_resolve_area_and_cat01`
(явно каже, який код не знайдено), яку можна буде виправити
адресно, без здогадок наосліп.

### 2026-09-13: japan_cpi (e-Stat) — перший живий прогін впав на DNS, не на коді
**Що сталось:** `collect_all.py` через Docker: усі 18 інших metric_id
(FRED/ECB/BOJ) успішно записались (0 нових/змінених — дані ті самі,
що вже були), `japan_cpi` впав з `NameResolutionError` на
`api.e-stat.go.jp` — контейнер не зміг резолвити хост у DNS. Не
timeout з'єднання, не 4xx/5xx від API — тобто не проблема коду чи
`appId`/`statsDataId`.
**Перевірено:** hostname `api.e-stat.go.jp` — правильний, звірено з
офіційною специфікацією e-Stat API v3.0. `estat_adapter.py` міняти
не треба.
**Рішення:** не чіпати код навмання. Позначено як відкладено до
повного перезбирання Docker з нуля (`down -v` + `up -d --build`),
заплановане користувачем на наступне оновлення — це саме собою може
усунути проблему (стейл DNS-кеш контейнера) без змін у коді.
**Наступний крок при повторному прогоні:** якщо DNS усе ще не
резолвиться після перезбирання — діагностика per `getent hosts`/
`nslookup` з контейнера і з хоста окремо (є в чаті сесії), можливо
знадобиться явний `dns:` блок у `docker-compose.yml`.
**Оновлення (той самий день, після перезбирання Docker):** DNS-проблема
зникла сама (транзієнтний глюк, як і підозрювалось) — `japan_cpi`
пройшов "успішно" (10/10 нових записів). Окремо того ж прогону разово
не зрезолвився `api.stlouisfed.org` для `initial_jobless_claims` —
той самий хост секундою раніше й пізніше відпрацював нормально, тобто
це не блокування конкретного хосту, а разовий збій DNS-резолвера
всередині контейнера. Якщо повторюватиметься систематично (не разово)
— тоді вже `dns:` блок у docker-compose.yml.

### 2026-09-13: japan_cpi — знайдено і виправлено реальний баг з виміром `tab`
**Що сталось:** хоча `japan_cpi` формально пройшов live-прогін,
логи показали "фальшиві ревізії" в межах одного запуску — та сама
дата отримувала кілька різних значень підряд (напр. `101.5 → 0.5 →
1.5`). Причина: `_resolve_area_and_cat01()` (стара назва методу)
резолвила тільки виміри `area`/`cat01`, ігноруючи `tab` — а ця
таблиця CPI публікує кілька рядків на кожен місяць під різними
кодами `tab`: сам рівень індексу (`指数`, ~101) окремо від %-зміни
р/р (`前年同月比`) і %-зміни м/м (`前月比`). Без фільтрації за `tab`
усі три потрапляли в один `metric_id=japan_cpi` і перезаписували
одне одного в `raw_observations` як послідовні "ревізії" одного й
того самого числа — хоча насправді це три різні величини.
**Виправлено:** `_resolve_area_and_cat01()` → `_resolve_dimensions()`
— тепер резолвить УСІ виміри таблиці (крім `time`), а не тільки два
захардкоджені. Для виміру з рівно одним значенням неоднозначності
нема — береться автоматично. Для виміру з кількома значеннями
потрібен запис у новому словнику `_DIMENSION_PREFERENCES` (людино-
зрозуміла назва потрібного варіанту); якщо для такого виміру запису
нема — явний `ValueError` з переліком доступних назв замість
мовчазного змішування даних. Додано `_DIMENSION_PREFERENCES["tab"]
= "指数"`.
**Другий шар захисту:** `normalize()` тепер дедуплікує по
`observed_at` — якщо для однієї дати після фільтрації запиту все ж
трапляються різні значення (напр. з'явиться новий tab-код, для якого
забудуть додати пріоритет), залишається перше значення, конфлікт
логується `WARNING`, а не мовчки пишеться в БД як черговий запис.
**Чому не було виявлено раніше:** фікстура `estat_meta_response.json`
була написана заздалегідь (мережа пісочниці Claude не мала доступу
до e-stat.go.jp) без виміру `tab` взагалі — хоча фікстура даних уже
містила `@tab: "01"` в кожному записі, ніхто (жоден тест) не
перевіряв, що буває, коли `tab` має більше одного значення. Додано
тести саме на цей сценарій (`test_resolve_dimensions_picks_preferred_values`,
`test_normalize_deduplicates_conflicting_values_for_same_date`) і нову
фікстуру `estat_cpi_response_mixed_tab.json`.
**Відкрите питання:** сам факт того, що live-прогін "пройшов успішно"
попри зіпсовані дані, показує загальний ризик — `run_collect.py`/
`collect_all.py` вважають запис успішним, якщо `store()` не кинув
винятку, навіть якщо кілька підряд "revision" для однієї дати в
одному прогоні — явна ознака проблеми з джерелом/адаптером, а не
легітимна ревізія від статистичного бюро. Варто розглянути (не
зроблено зараз): попередження на рівні `collect_all.py`, якщо
кількість нових revision для одного metric_id в одному прогоні
перевищує якийсь поріг (напр. >1 на дату).

### 2026-09-14: companies/ (SEC EDGAR) — спершу скринінг акцій, потім збір даних (не навпаки)
**Рішення:** перед тим як будувати `companies/`-адаптер і тягнути
фундаментальні дані по конкретних тикерах, спершу визначаємо й
документуємо критерії відбору акцій (`docs/screening-criteria.md`) і
джерело котирувань для них. `companies/` збиратиме дані тільки по
вже відфільтрованому списку ~10 тикерів, а не по довільному чи
дефолтному watchlist.
**Чому:** початковий план ("візьмемо AAPL/MSFT як seed") — це просто
моніторинг топ-N компаній, а не інструмент пошуку акцій для торгів.
Мета проєкту — знаходити кандидатів за критеріями, тому спершу потрібен
сам скринер (і дані для нього), а вже тоді — збір деталізованих
фундаментальних даних по відібраних тикерах.
**Наслідок для архітектури `companies/`:** на відміну від FRED/ECB/BOJ/
e-Stat, де `METRICS` — фіксований словник конкретних показників,
`companies/` матиме `METRICS`-словник **концептів** (us-gaap tags:
`revenue`, `eps_diluted`, `net_income`, `shares_outstanding`), а
тикер — параметр виклику, не хардкод. `metric_id` збирається як
`{ticker}_{концепт}` (напр. `aapl_revenue`). CIK резолвиться через
bulk-файл `company_tickers.json` (з кешем), а не хардкодиться.
**Відкинуті альтернативи:** захардкодити 2-3 тикери як seed і рухатись
далі — відкинуто, бо не відповідає меті "знаходити акції для торгів",
а не просто моніторити довільний список.

### 2026-09-14: Джерело котирувань для скринінгу — Stooq, свідомий виняток з "official sources first"
**Рішення:** для ціни/обсягу торгів (потрібні для ліквідності,
капіталізації, valuation у скринінгу) використовуємо Stooq: без
API-ключа, простий CSV-формат, без офіційної реєстрації.
**Чому:** на відміну від макропоказників (центробанки/статвідомства —
безкоштовні офіційні першоджерела), для біржових котирувань окремих
акцій безкоштовного офіційного джерела не існує — біржі (NYSE/NASDAQ)
монетизують realtime/історичні фіди. Це перший випадок в проєкті, коли
принцип "official sources first" свідомо не застосовується — задокументовано
явно, а не мовчки обійдено.
**Розглянуті альтернативи:**
- Alpha Vantage — відкинуто, free tier зараз лише 25 запитів/день,
  замало навіть для щоденного моніторингу ~10-500 тикерів (залежно
  від ширини universe для скринінгу).
- Finnhub — 60 запитів/хв, але історичні свічки на free tier часто
  урізані/платні.
- Twelve Data — 800 запитів/день, прийнятний резерв, але потребує
  API-ключа й реєстрації; лишається запасним варіантом, якщо Stooq
  виявиться нестабільним на практиці.
**Ринкова капіталізація:** не тягнеться окремим полем з джерела
котирувань — рахується як ціна (Stooq) × `EntityCommonStockSharesOutstanding`
(SEC EDGAR, dei-таксономія). Тримає EDGAR джерелом істини там, де це
можливо, і мінімізує залежність від неофіційного джерела.

### 2026-09-14 (ревізія того самого дня): Stooq → Alpaca Markets — одне джерело замість двох окремих рішень
**Рішення:** попередній запис вище (Stooq для котирувань) скасовано
до того, як з'явився будь-який код — замінено на **Alpaca Markets**
(брокер-дилер, Trading API + Market Data API). Alpaca закриває одразу
і котирування, і питання джерела universe-списку з
`docs/screening-criteria.md`, яке інакше довелось би розв'язувати
окремо (scraping Wikipedia-таблиці S&P 500 чи `company_tickers.json`).
**Чому:**
- `GET /v2/assets` (Trading API) — готовий список тикерів NYSE/NASDAQ
  з полями `tradable`/`marginable`/`shortable`, придатний як universe
  напряму, без scraping.
- `/v1beta1/screener/stocks/movers` і `.../most-actives` (Market Data
  API) — топ gainers/losers і найактивніші за обсягом, безкоштовно.
- `/v2/stocks/bars`, `/v2/stocks/{symbol}/snapshot` — історичні й
  поточні OHLCV, закриває ту саму потребу, що мав закривати Stooq.
- Один акаунт/ключ замість трьох незалежних джерел (Stooq +
  Wikipedia-scrape + окремий рішення про screener-сигнали).
- Alpaca — зареєстрований брокер-дилер (регульована структура), а не
  анонімний CSV-вендор — трохи ближче до "official", хоча формально
  все одно не державне джерело (той самий виняток з "official sources
  first", тільки з іншим постачальником).
**Ціна питання:** потрібна реєстрація реального акаунту (email,
телефон) — на відміну від Stooq, де взагалі нічого не треба.
Користувач зареєструється і додасть `APCA_API_KEY_ID`/
`APCA_API_SECRET_KEY` (paper-акаунт) у `.env`.
**Відкинуті альтернативи:** лишити Stooq для котирувань + окремо
вирішувати universe-джерело (Wikipedia/`company_tickers.json`) —
відкинуто, бо Alpaca закриває обидві потреби одним джерелом і одним
акаунтом, менше рухомих частин для підтримки.
**Відкрите питання:** чи буде `most-actives`/`movers` (моментум-
орієнтований скринер) основним джерелом universe, чи тільки
допоміжним сигналом поверх повного `/v2/assets` (весь тикер-лістинг)
— саме це рішення описано в `docs/screening-criteria.md`, ще не
закрито.

### 2026-09-14/20: Alpaca відкочено — Stooq (котирування) + SEC `company_tickers_exchange.json` (universe), без акаунтів і геозалежностей
**Рішення:** попередня ревізія (Alpaca замість Stooq) скасована.
Повертаємось до **Stooq** для ціни/обсягу/історії — без ключа, без
акаунту, без реєстрації. Для universe-списку — замість `/v2/assets`
(Alpaca) використовуємо `https://www.sec.gov/files/company_tickers_exchange.json`
(офіційний SEC-файл, той самий хост, що вже потрібен для `companies/`):
містить `cik`/`name`/`ticker`/`exchange` для всього лістингу
NYSE/NASDAQ, підтверджено живим дослідженням структури файлу.
**Чому відкотили Alpaca:** користувач повідомив, що `alpaca.markets`
і його документація взагалі не відповідають (не 403/помилка API — сайт
не завантажується). У Alpaca офіційно є (недокументовані публічно)
географічні обмеження — довідкова сторінка "Countries Alpaca is
available" прямо каже писати в support, щоб дізнатись, чи підтримується
країна. Точна причина (гео-блок з боку Alpaca чи локальна мережева
проблема) не підтверджена тестом з іншої мережі — але з огляду на
невизначеність і на те, що Alpaca взагалі потребує реєстрації реального
акаунту (на відміну від Stooq/EDGAR), продовжувати з нею недоцільно:
ризик витратити час на джерело, яке може бути статично недоступне з
України.
**Наслідок:** нове рішення виявилось кращим за обидва попередні варіанти
одразу — `company_tickers_exchange.json` офіційніше за Alpaca (це
пряме SEC-джерело, не брокер) і не потребує scraping Wikipedia (як
розглядалось ще до Alpaca). Жодного акаунту не потрібно ні для
universe, ні для котирувань.
**Відкинуті альтернативи:** розбиратись з доступністю Alpaca далі
(тестувати з іншої мережі/VPN) — відкинуто, недоцільно витрачати час,
коли є однаково робочий варіант без цієї залежності взагалі.

### 2026-09-20: Закриті всі відкриті питання скринінгу — пороги, ребалансування, score, звужений universe + news-канал
**Рішення (пороги Tier A-C, фінально):**

Tier A — ліквідність:
- Біржа: NYSE або NASDAQ
- Ціна акції: > $10 (не $5 — професійний скринінг зазвичай тримає вищу
  планку, ніж мінімальна відсічка penny-stock, щоб додатково
  відфільтрувати спекулятивний шум)
- Ринкова капіталізація: > $10 млрд (large-cap — "топові", а не просто
  "не мікрокап"; узгоджується з рішенням нижче звузити universe до
  S&P 500)
- Середній обсяг торгів (3 міс): > $10 млн/день у доларовому виразі
  (не в к-сті акцій — доларовий обсяг коректно порівнює акції з різною
  ціною, стандартна практика в професійному скринінгу)

Tier B — фундаментальна якість (планка піднята проти чернетки):
- `NetIncomeLoss` > 0 останні 4 квартали поспіль
- Зростання виручки YoY > 10% (було 5% — "перспективні" мають рости
  помітно, не символічно)
- Зростання EPS (diluted) YoY > 15%
- К-сть акцій в обігу не зросла > 3% YoY (було 5% — менша толерантність
  до розмиття для топових кандидатів)
- Борг: Liabilities/Assets < 0.6 (`Liabilities` / `Assets`, us-gaap) —
  новий критерій, у чернетці був лише позначений словесно, без цифри

Tier C — valuation:
- P/E (ціна/EPS diluted TTM): 10–35 (звужено з 5-40 — і надто дешеве
  (value trap risk для growth-скринера), і надто дороге відсікається
  жорсткіше)
- P/S (капіталізація/виручка TTM): < 8 (було <10)
- PEG (P/E ÷ EPS growth %) < 2 — новий критерій, стандартний
  growth-at-reasonable-price фільтр, страхує від "росте на 15%, але
  P/E вже 60"

**Рішення (ребалансування):** синхронно зі звітністю — перезбір і
переранжування списку після кожного циклу 10-Q (щокварталу, коли
компанії зі списку публікують нові квартальні цифри).

**Рішення (composite score, фінальна формула):** серед кандидатів, що
пройшли всі hard-фільтри Tier A+B+C, ранжуємо за:
```
score = 0.35 × percentile(revenue_growth_yoy)
      + 0.30 × percentile(eps_growth_yoy)
      + 0.20 × percentile(-P/E)              # нижчий P/E → вищий ранг
      + 0.15 × percentile(avg_dollar_volume)  # ліквідність як tie-breaker
```
`percentile(x)` — процентильний ранг (0-100) значення `x` серед усіх
кандидатів, що пройшли hard-фільтри в поточному прогоні (не серед
усього universe) — уникає проблем масштабу різних метрик без
довільної нормалізації. Топ-10 за підсумковим `score`. Ваги (35/30/20/15)
— стартова точка за принципом "зростання важливіше за дешевизну для
growth-кандидатів", підлягають калібруванню після першого реального
прогону на живих даних.

**Рішення (universe звужено):** базовий universe — constituenти S&P 500
(джерело: `https://raw.githubusercontent.com/datasets/s-and-p-500-companies/main/data/constituents.csv`,
community-датасет з тикер+назва+сектор; периодично оновлюється, не
щодня), а не весь `company_tickers_exchange.json` (кілька тисяч
тикерів). Це і закриває "не потрібно скринити весь мотлох" (S&P 500 —
вже попередньо якісний, ліквідний, великокапітальний пул), і суттєво
знижує к-сть запитів на Stooq за прогін скринера (~500 замість кількох
тисяч) — той самий момент, що піднімався як відкрите питання №4 (обсяг
запитів). Tier A вище (капіталізація >$10 млрд, ціна >$10) далі звужує
навіть у межах S&P 500.

**Рішення (news-канал додавання):** окремо від планового щоквартального
прогону — акції, що спливають у новинах (майбутнє джерело `news/`,
GDELT — ще не побудовано), можуть потрапити в список поза чергою, але
**тільки після проходження Tier B** (фундаментальна перевірка) —
новинний привід сам по собі не є причиною додавання, лише тригер для
позачергової перевірки конкретного тикера. Якщо фундаментал
проходить — акція додається до списку до наступного планового
ребалансування; якщо ні — відкидається, у список не потрапляє.
**Наслідок для архітектури:** `companies/` (SEC EDGAR) мусить вміти
приймати одиничний тикер поза плановим batch-прогоном (уже так
спроєктовано — тикер є параметром виклику, не хардкодиться), тож
додаткової роботи тут не потрібно; коли дійдемо до `news/`, він просто
викликатиме той самий шлях перевірки.

### 2026-09-20: `quotes/stooq_adapter.py` написано; run_collect.py узагальнено під динамічні тикери
**Рішення:** `StooqAdapter` реалізовано за узгодженою в
`docs/screening-criteria.md` схемою — `FIELDS: dict[str, str]`
фіксує лише поля бару (`close`, `volume`), тикер — параметр
конструктора, `metric_id` збирається як `{ticker}_{field}`.
`run_collect.py` узагальнено: додано `--ticker` (альтернатива
`--metric`, взаємовиключні) і `id_kwarg` у `ADAPTERS`-реєстрі
("metric_id" для macro/*-адаптерів, "ticker" для stooq) — той самий
механізм знадобиться для `companies/` (SEC EDGAR), коли дійдемо до
нього, без повторної переробки CLI. Для `--ticker` джерело (`--source`)
вимагається явно — автовизначення за фіксованим METRICS-словником
тут не працює, бо тикери не з фіксованого списку.
**`fetch()` ігнорує `limit`:** Stooq не підтримує "останні N рядків"
як параметр запиту — тільки діапазон дат (`d1`/`d2`). `run_collect.py`
завжди викликає `collect(limit=...)` незалежно від джерела, тож
`fetch()` приймає `limit`, але не застосовує (тільки лог на debug-рівні) —
інакше довелось би або ламати спільний CLI-контракт для одного джерела,
або вигадувати неточне перетворення "N рядків" у діапазон дат.
**Схема:** `db/schema.sql` — `stooq` додано в `sources`
(`category='quotes'`, `source_type='aggregator'`), коментар над
`category` розширено значенням `quotes`.
**Тести:** 10 нових (`tests/test_stooq_adapter.py`) на фікстурі
`stooq_aapl_response.csv` — конструктор/валідація тикера (в т.ч.
клас акцій на кшталт `BRK-B`), парсинг close/volume, пропуск
відсутніх значень, явний `ValueError` на порожню/неочікувану
відповідь (той самий принцип, що в `estat_adapter`), URL/параметри
`fetch()`, ігнорування `limit`. Разом з існуючими — 53/53 проходять
локально.
**Не зроблено (свідомо, наступний крок):** живий прогін проти
реального Stooq API. `stooq.com` не входить у дозволені домени
мережі пісочниці Claude (на відміну від `sec.gov`, `data-api.ecb.europa.eu`
тощо) — не вдалось навіть підтвердити точний формат CSV живим
запитом під час розробки (спирався на задокументовану поведінку з
кількох незалежних джерел: заголовок `Date,Open,High,Low,Close,Volume`,
без офіційної специфікації від самого Stooq). Перший живий запуск
користувача — це фактична перевірка, а не формальність, як і для
кожного попереднього джерела.

### 2026-09-20 (продовження, живий тест): помилковий висновок про apikey — спростовано реальним файлом, відкочено
**Що сталось:** перший живий запуск користувача (`--source stooq
--ticker AAPL`) дав `404 Client Error`. Замість попросити користувача
показати, що саме повертає сайт, я пішов шукати причину в вебі й
знайшов два сторонні джерела, які стверджували, що Stooq з квітня
2026 вимагає `apikey` через капчу — і на основі ЦЬОГО, не живих
даних, переписав адаптер під обов'язковий ключ. Користувач слушно
вказав, що я міняю код і "палю токени", не розібравшись з реальним
API — жодної капчі на сторінці немає.
**Що показав реальний файл (користувач завантажив живий CSV з
`https://stooq.com/q/d/l/?s=aapl.us&i=d&c=1`):** ключ не потрібен
узагалі, досить параметра `c=1`, якого в моєму URL не було (звідси,
ймовірно, і 404 — не з apikey). Друга, важливіша знахідка, яку
неможливо було отримати без реального файлу: **роздільник CSV —
`;` (крапка з комою), не `,`** — Stooq польський сервіс, це їхній
регіональний формат. Перша версія адаптера з комою як роздільником
мовчки зламала б парсинг (кожен рядок читався б як одне поле) —
цю поломку жоден пошук у вебі показати не міг.
**Виправлення:** `StooqAdapter` повернуто до без-ключового вигляду,
`fetch()` — параметри `s`/`i`/`c` (без `apikey`), `normalize()` —
`csv.DictReader(..., delimiter=";")`. Фікстура тестів замінена на
фрагмент реального файлу (не вигадані дані). Перевірено наскрізно на
всій реальній історії AAPL (10589 днів, 1984–2026) — 21178 записів,
0 дублікатів, 0 помилок парсингу. `run_collect.py`/`.env.example` —
відкочено `STOOQ_API_KEY`.
**Урок:** для перевірки живої поведінки зовнішнього API вебпошук —
не заміна реальному запиту/файлу. Коли є розбіжність (404, неочікуваний
формат) — правильний перший крок питати в користувача, що саме
повертає джерело, а не переписувати код під здогадку з пошуку.

### 2026-09-20 (продовження): Stooq відкинуто (бот-захист), Twelve Data — джерело котирувань, підтверджено живим запитом
**Що сталось після фіксу delimiter/c=1:** той самий URL, що спрацював
у браузері користувача, далі падав з контейнера (404). Діагностика
(порівняння default vs. browser User-Agent) показала: без
браузер-подібного User-Agent Stooq віддає порожній 404; з ним —
200, але HTML-сторінку "This site requires JavaScript to verify your
browser" замість CSV. Це JS-based бот-захист (Cloudflare-подібний) —
`requests` в принципі не може його пройти без headless-браузера
(Playwright/Selenium). Це вже другий безкоштовний "без реєстрації"
варіант поспіль, що виявився нежиттєздатним на практиці (перший —
Alpaca, гео-блок).
**Рішення:** Stooq остаточно відкинуто. Замість третього раунду
"знайти джерело через пошук і сподіватись" — попросив користувача
самостійно зареєструватись і перевірити **Twelve Data** (був
запасним варіантом ще з першого запису про вибір джерела котирувань)
до того, як писати код. Користувач зареєструвався, зробив живий
запит `time_series?symbol=AAPL&interval=1day&outputsize=5&apikey=...`
з контейнера — 200, реальні дані. Адаптер написано вже під
підтверджений формат відповіді (JSON, `values[]` з
`datetime/open/high/low/close/volume` як рядки), а не під
припущення.
**Деталі:** реєстрація — email+пароль, без капчі, ключ одразу в
кабінеті (на відміну від Stooq apikey, який спершу помилково
вважався потрібним через капчу — див. запис вище, "помилковий
висновок про apikey"). Ліміти free tier: ~8 запитів/хв, 800/добу.
Помилки API повертаються з HTTP 200 і `status: "error"` в тілі
(не HTTP-кодом помилки) — `normalize()` перевіряє це явно, бо
`raise_for_status()` у `fetch()` такого не ловить.
**`db/schema.sql`:** джерело `stooq`, додане раніше в
`sources`-таблицю користувача через `apply_schema.py`, лишається
там як невикористовуваний історичний рядок (append-only-філософія
проєкту стосується `raw_observations`, не метаданих джерел; DELETE
тут не критичний, але й не потрібен — рядок нешкідливий). Нове
джерело `twelvedata` додано окремим INSERT.
**Урок (продовження попереднього запису):** для будь-якого джерела,
що претендує на "без реєстрації/без ключа", варто питати користувача
перевірити це самому одним запитом, перш ніж писати адаптер — це
третій раз поспіль, коли одна жива перевірка користувача дала більше
сигналу за годину дослідження через пошук.

### 2026-09-20 (продовження): `companies/sec_edgar_adapter.py` написано
**Рішення:** реалізовано за схемою з попереднього запису (2026-09-14,
"companies/ — спершу скринінг") — `METRICS` словник концептів
(revenue/net_income/eps_diluted/shares_outstanding/assets/liabilities
→ us-gaap/dei tag), тикер — параметр конструктора, CIK резолвиться
через `company_tickers.json` (кеш на процес) або передається явно.
Параметр названо `api_key` (не `user_agent`), щоб збігатися з
генеричним викликом у `run_collect.py` — той самий трюк, що вже
використано в `EstatAdapter`.
**Новий захист (наперед, не постфактум — урок з japan_cpi застосовано
одразу, а не після живого багу):** XBRL duration-концепти можуть мати
квартальне й кумулятивне (з початку року) значення з однаковим "end",
різним "start" — `normalize()` фільтрує за довжиною періоду (75-100
днів), лишає тільки квартальні. Без цього злиття виглядало б як
фальшиві ревізії — той самий клас багу, що вже був у `estat_adapter`.
**Чесно позначено нетестоване:** формат companyconcept підтверджено
кількома незалежними технічними джерелами (в т.ч. дзеркалом офіційної
документації SEC), але НЕ живим запитом — `sec.gov`/`data.sec.gov`
немає в дозволених доменах пісочниці Claude (на відміну від
`raw.githubusercontent.com`, який вдалось перевірити напряму для
`collect_universe.py` того ж дня). Перший прогін користувача —
фактична перевірка, а не формальність.
**Тести:** 11 нових на фікстурах, побудованих за задокументованою
формою (не live-дамп) — резолюція CIK, фільтр кумулятивних записів,
фільтр форм (тільки 10-K/10-Q), 404-толерантність по концепту,
явний ValueError коли всі концепти відсутні. Разом — 63/63.

### 2026-09-21: sec_edgar_adapter — дедуплікація дублікатів по даті (2-й клас проблеми з тим самим коренем, що japan_cpi)
**Що сталось:** живий тест користувача на AAPL показав "ping-pong"
ревізії — той самий день (напр. `aapl_eps_diluted` за 2012-12-29)
то `1.97`, то `13.81`, то знову `1.97`. Причина: SEC для того самого
кварталу інколи повертає ДВА записи з різним `val` — типово після
спліту акцій, коли пізніше подання перераховує EPS минулих періодів
під нову к-сть акцій; для instant-концептів без спліту (`assets`,
`liabilities` за 2015-09-26) — просто невеликі розбіжності між
поданнями. Попередній фільтр (форма + довжина періоду, запис від
2026-09-20) цього не ловив — обидва записи однаково проходили форму
й тривалість, різнились лише значенням.
**Виправлення:** дедуплікація по `observed_at` — коли кілька записів
припадають на одну дату, береться той, що з найпізнішим `filed`
(найактуальніше подання), решта відкидається. Той самий принцип
"один визначений результат на ключ, не змішування конкурентів", що
і в фіксі `japan_cpi` (сесія 9) — інша конкретна причина дублів, той
самий клас рішення.
**Тести:** новий кейс на реальних значеннях з живого логу (`13.81`/
`1.97` за 2012-12-29) — підтверджує, що лишається один запис,
береться пізніше подання. Разом — 64/64.
**Свідомий компроміс:** беремо найактуальніше подання, не
"як спочатку повідомлено". Для growth-скринінгу (YoY EPS/revenue)
це правильніший вибір — порівнянність цифр у одних одиницях
важливіша за точну відповідність оригінальній публікації.

### 2026-09-23: Масовий збір companies/ (SEC EDGAR) + перший код analysis/ — Tier A
**Рішення (companies/, масовий збір):** `collect_companies_universe.py` —
CIK береться з `constituents.csv` (колонка CIK, підтверджено раніше),
не з окремого лукапу `company_tickers.json` на кожен тикер — уникає
503 зайвих запитів за той самий файл. `SecEdgarAdapter.fetch()` тепер
витримує паузу 0.15с між запитами концептів (SEC ліміт 10/сек) —
додано наперед, до масового прогону, а не постфактум після 429.
**Рішення (analysis/, перший код):** `analysis/screening/tier_a.py` —
Tier A (ліквідність) з `docs/screening-criteria.md`. Читає лише з
raw_observations (через `v_current_values`/`v_observations_latest_revision`),
жодних звернень до зовнішніх API — CLAUDE.md, правило розділення
data-ingestion/analysis. Критерії вже узгоджені раніше (2026-09-20):
ціна >$10, капіталізація >$10 млрд (ціна × shares_outstanding),
середній доларовий обсяг (63 дні) >$10 млн/день.
**Архітектурне рішення:** сама логіка порогів винесена в чисту функцію
`passes_tier_a()` окремо від SQL-запитів до БД — тестується без живої
БД (6 тестів). Сам SQL — ні (TimescaleDB немає в пісочниці Claude),
перший живий прогін користувача це підтвердить, як і для кожного
попереднього джерела.
**Технічна деталь:** `analysis/` — окрема top-level тека від
`data-ingestion/` (дефіс у назві не дозволяє імпортувати як пакет) —
`tier_a.py` додає `data-ingestion/` у `sys.path` вручну для
перевикористання `common.db.get_connection()`, а не дублює
підключення до БД окремим кодом.

### 2026-09-24: shares_outstanding відсутній для ~110/501 компаній — пробіл джерела, не баг
**Що сталось:** Tier A відсіяв 110 тикерів (з 491) як "немає
shares_outstanding з SEC EDGAR" — серед них великі відомі компанії
(ABT, DIS, CRM, BIIB). Перевірено напряму: SEC для цих компаній
повертає 200 на `companyconcept/.../EntityCommonStockSharesOutstanding`,
але `units.shares` — порожній `{}` (не список з записами). Мій
адаптер це вже коректно обробляв (`if not entries: continue` — порожній
dict теж falsy), тож це не баг коду, а реальний пробіл джерела:
ймовірно, компанії з кількома класами акцій звітують
shares_outstanding по класах окремо (dimension/member), а не єдиним
агрегатом, який дає простий `companyconcept`-запит.
**Рішення:** не фіксимо зараз. 365/491 (74%) — робочий результат для
Tier A; виключення без даних про капіталізацію — консервативно
правильно (не вгадуємо капіталізацію без shares). Занотовано як
можлива майбутня доробка: `CommonStockSharesOutstanding` (us-gaap,
без dei) як fallback-тег, за тим самим принципом, що вже застосовано
для revenue нижче.

### 2026-09-24: revenue-тег застарів для AAPL і, ймовірно, багатьох компаній — виправлено multi-tag fetch до Tier B
**Що сталось:** перед Tier B виявив (без нового живого тесту —
з уже наявних даних AAPL у БД), що `aapl_revenue` зупиняється на
2018-09-29 — сигнал, що описаний як "відомий пробіл" ще в записі про
sec_edgar_adapter (2026-09-20), матеріалізувався. Причина:
AAPL (і, найімовірніше, велика частка компаній) перейшли з
`Revenues` на `RevenueFromContractWithCustomerExcludingAssessedTax`
(пост-ASC 606), і стара позначка просто перестала оновлюватись
в пізніших поданнях.
**Виправлення:** `METRICS` тепер `dict[str, list[tuple[taxonomy, tag]]]`
замість одного тега на концепт — `revenue` фетчить обидва варіанти,
`fetch()` збирає результати в `results[suffix] = [payload, ...]`,
`normalize()` зливає записи з усіх варіантів одного суфікса через ту
саму дедуплікацію по даті (найпізніший `filed` виграє), що вже
існувала для спліт-дублікатів — дублікат між тегами обробляється
тим самим механізмом, без окремого коду. Виправлено ДО живого
прогону Tier B, а не постфактум після дивних результатів.
**Тести:** новий кейс на злитті двох тегів у один часовий ряд
(старі дані з `Revenues`, нові — з `RevenueFromContract...`,
підтверджено, що обидва потрапляють під один `metric_id`). Разом —
65/65 (data-ingestion).
**Не зроблено:** такий самий фолбек для `shares_outstanding`
(запис вище) — окрема, ще не вирішена задача.

### 2026-09-24: analysis/screening/tier_b.py — перший багатокроковий скринінг-код
**Рішення:** `passes_tier_b()` — чиста функція (як і `passes_tier_a()`),
приймає часові ряди (net_income, revenue, eps, shares) і останні
значення (liabilities, assets), повертає (пройшов, деталі). YoY
рахується як (найновіше - рік_тому)/|рік_тому|, з пошуком "запису
рік тому" в межах допуску ±20 днів навколо рівно 365 днів (квартали
не завжди рівно 91.25 дні одна від одної між компаніями).
**Порядок перевірки критеріїв:** profitability → revenue YoY → EPS
YoY → dilution → L/A — рання зупинка при першому провалі (деталі для
пізніших критеріїв просто відсутні в результаті, не `None`/фейкові
нулі) — economить SQL-запити на явно непрохідних тикерах, коли
run_tier_b() під'єднає це до реальної БД.
**Тести:** 14 нових на чистій логіці (`all_positive_last_n`,
`yoy_growth`, `passes_tier_b` — усі 4 критерії окремо й разом), без
БД. Разом analysis/ — 20/20. SQL-шар (`_series`) не тестований —
перший живий прогін користувача це перевірить.

## Приклад запису (видалити, коли з'явиться перше реальне рішення)
### 2026-08-24: Формат сховища сирих даних
**Рішення:** append-only таблиці, без update/delete для фактичних даних.
**Чому:** потрібна можливість порівнювати "що знали тоді" з реальністю
пізніше; видалення/перезапис ламає цю можливість назавжди.
**Відкинуті альтернативи:** тримати тільки останнє значення кожного
показника (простіше, але втрачає історію ревізій даних, яка сама по собі
є сигналом).
