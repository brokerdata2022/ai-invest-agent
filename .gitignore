# Секрети — НІКОЛИ не комітити
.env
.env.local
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
. pytest_cache/

# IDE
.vscode/
.idea/
*.kate-swp

# OS
.DS_Store

# Дані (якщо будете зберігати сирі файли локально, а не тільки в БД)
data/raw/
*.db
*.sqlite3

# Локальні дампи БД — не джерело істини, db/schema.sql та raw_observations нею є
backup.sql
