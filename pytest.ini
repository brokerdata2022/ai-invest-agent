[pytest]
; data-ingestion не є валідним іменем Python-пакета (дефіс), тому
; додаємо його вміст у sys.path напряму — це дозволяє тестам і коду
; імпортувати "common" та "macro" однаково, незалежно від cwd.
pythonpath = data-ingestion reporting
testpaths = data-ingestion/tests reporting/tests
