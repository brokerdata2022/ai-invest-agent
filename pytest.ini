[pytest]
; data-ingestion не є валідним іменем Python-пакета (дефіс), тому
; додаємо його вміст у sys.path напряму — це дозволяє тестам і коду
; імпортувати "common"/"macro"/"companies"/"quotes" однаково, незалежно
; від cwd. "analysis" додано так само, щоб тести могли імпортувати
; "screening.tier_a" тощо без sys.path.insert() у кожному файлі.
pythonpath = data-ingestion reporting analysis
testpaths = data-ingestion/tests reporting/tests analysis/tests
